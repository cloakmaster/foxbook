{
  "name": "@foxbook/adapter-email",
  "version": "0.0.0",
  "private": true,
  "description": "Email verification adapter (Resend).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "biome lint ."
  },
  "dependencies": {
    "resend": "^6.12.2"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
