{
  "name": "@foxbook/adapter-dns",
  "version": "0.0.0",
  "private": true,
  "description": "DNS TXT challenge Tier-2 adapter (Cloudflare DoH via fetch).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "biome lint ."
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
