{
  "name": "@foxbook/adapter-endpoint-challenge",
  "version": "0.0.0",
  "private": true,
  "description": "Endpoint signed-nonce Tier-2 verification adapter (Ed25519 over canonical JSON via fetch).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "biome lint ."
  },
  "dependencies": {
    "@foxbook/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
