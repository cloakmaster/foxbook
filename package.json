{
  "name": "foxbook",
  "version": "0.0.0",
  "private": true,
  "description": "Foxbook — verifiable agent identity primitive for A2A and MCP. RFC 9162-shaped public transparency log. Apache 2.0.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check:core-isolation": "node scripts/check-core-isolation.mjs",
    "check:no-deskduck": "node scripts/check-no-deskduck.mjs",
    "generate:types": "node scripts/generate-types.mjs",
    "check:generated": "node scripts/check-generated.mjs",
    "capture:merkle-vectors": "pnpm --filter @foxbook/core capture:merkle-vectors",
    "smoke:tier1": "pnpm --filter @foxbook/api smoke:tier1 --",
    "smoke:revoke": "pnpm --filter @foxbook/api smoke:revoke --",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "^22.10.0",
    "husky": "^9.1.7",
    "json-schema-to-typescript": "^15.0.3",
    "lint-staged": "^15.2.10",
    "quicktype-core": "^23.0.170",
    "turbo": "^2.9.16",
    "typescript": "^5.7.2"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,md}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  }
}
