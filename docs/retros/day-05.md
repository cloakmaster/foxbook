# Day 5 retro — 2026-04-23

**Slipped.** Pre-pop scraper skeleton (apps/scrapers/github/) — explicitly deferred to Day 5.5 / Day 6 in the Day-5 kickoff v1.1 and confirmed at plan-paragraph time; did not ship today. Migration 0001 + 0002 against Neon dev also slipped from this session — the harness correctly denied agent-side `db:migrate` per the "via Benjamin's terminal" clause in the kickoff, so apply is pending Benjamin's local run before the first live-Neon `verify-gist` call. Taxonomy **scraped-AgentCard cross-ref** slid to week 2 (the scraper would have fed it); today's freeze is hand-curated from foundation §6.8 + A2A v0.3.0 SDK skill examples. Type-generation for the capability enum **never shipped** and will not — generating it would embed banned capability literals into `packages/types-ts/` (a service-agnostic zone), so `check-generated` stays at 8 files instead of the kickoff's aspirational 10. Net: Day 5 shipped ~80% of kickoff scope in three PRs (#12, #13, #14) on calendar Day 3 afternoon/evening — still ahead of calendar.

**Bench measured.** No new bench numbers; the Day-5 surfaces (Worker HTTP routes, Gist polling, claim-flow handler) aren't SLA-relevant until they hit real traffic on staging (week 2+). The existing Merkle-append CSV (`ops/bench-results/2026-04-23-merkle-append.csv`) carries one Day-4 row; a second row lands whenever the next manual `pnpm --filter @foxbook/core bench:merkle-append` runs. Test count is the closest quantitative signal: **52 validator tests** (up from 47), **12 Gist adapter tests** (new), **24 apps/api tests** (up from 13 — 11 new claim-flow tests, every negative path asserts `appendSpy.callCount === 0`), **24 transparency Worker tests** (new), **104 Python tests** (no Python changes today). The identity-guard test (Gist adapter: mismatched owner → `fetchCount === 0`) is the load-bearing authorization-boundary regression guard; if it ever flips to `1`, a credential-forgery surface is open.

**Now known that wasn't.** (1) `packages/db` client split works cleanly with option (b) schema-only export — `createNodeClient` (postgres-js) and `createEdgeClient` (neon-http) both return Drizzle clients bound to the same `schema.*`, and the additive `signingKey?: Uint8Array` on `MerkleRepositoryOptions` lets the read-only transparency Worker instantiate without a key (append throws at runtime if called there; no silent zero-byte sign). (2) `apps/transparency`'s runtime-on-Workers validation is deferred to a staging smoke test — in-process `vitest` with a fake `MerkleRepository` covers the route contract + every Cache-Control header without spinning up miniflare; the actual neon-http + drizzle behavior under a Workers isolate is a staging-only unknown until `wrangler publish` runs (blocked on `CLOUDFLARE_ACCOUNT_ID` + DNS binding). (3) Claim-flow concurrency is **correct-by-construction via a DB partial unique index** (`claims_asset_uniq_idx ON (asset_type, asset_value) WHERE both NOT NULL`), not via app-layer racing — the Gist identity guard is a *separate* auth boundary that catches the alice-claims-samrg472 scenario before any fetch. Two independent defenses, one per layer. (4) The capability taxonomy + core-isolation interaction was non-obvious: bare nouns ("translation", "summarization", "ocr", …) are banned literals, so compound forms like "text-translation" and "audio-transcription" carry both modality context AND immunity from the regex check — but the tests can't literal-embed the ban list either, so the meta-test loads it from `core-isolation.config.json` at runtime and cross-checks. (5) `apps/api`'s tsconfig needed the same `rootDir` removal fix that `packages/db` got in Day 4 once we imported `@foxbook/core` from there — the tsconfig.base path mapping points directly at source files outside the app's rootDir. Flagged as a pattern; future workspaces that import `@foxbook/core` via paths will hit the same issue.

**Day-6 queue** (named so Day 6 doesn't scramble): apply Migration 0001 + 0002 to Neon dev (Benjamin); staging migration; `adapters/dns/` + `adapters/endpoint-challenge/` for Tier 2 DNS TXT + signed-nonce round-trip; revocation harness start (first recovery-key-signed revocation leaf — will require a v1.1 additive bump to `tl-leaf.v1.json` since v0 taxonomy doesn't carry a `revocation` shape yet); pre-pop scraper skeleton (if time after Tier 2 core). Day-7 close target stays the same: key rotation / revocation e2e test passing on staging.
