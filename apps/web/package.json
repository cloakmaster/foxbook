{
  "name": "@foxbook/web",
  "version": "0.0.0",
  "private": true,
  "description": "Static landing page for foxbook.dev. Deployed via Cloudflare Pages.",
  "scripts": {
    "typecheck": "echo 'static site, no typecheck' && exit 0",
    "test": "echo 'no tests' && exit 0",
    "lint": "biome lint ."
  }
}
